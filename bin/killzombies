#!/bin/bash

echo "Killing zombies"
echo "---------------"

for i in $(ps aux | awk '{ print $8 " " $2 }' | grep -w Z | cut -c3-30); do
	echo "$(ps -p $i -o comm= | awk '{ print $1 }') ($i)"
	kill -9 "$i"
done

echo "done..."
