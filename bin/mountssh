#!/bin/bash

# -C : uses compression transfer 
# -o allow_other : allow non-root read/write access
# -o follow_symlinks : Follows symlinks
# -p : Port
sudo sshfs -o allow_other -o follow_symlinks -C -p 8090 lemones@192.168.1.138:/home/lemones/ /home/lemones/Server/
