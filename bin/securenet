#!/bin/bash


if pgrep "openvpn" > /dev/null
then
	echo "[EE] openvpn already running..."
else
	echo "[*] starting openvpn..."
	nohup sudo openvpn --config /etc/openvpn/frankfurt.ovpn &>$HOME/.openvpn.log $
	echo "[*] openvpn running..."
	wait
	sleep 2
	echo "[*] Exiting openvpn..."
fi
